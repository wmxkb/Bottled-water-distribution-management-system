<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- namespace属性相当于映射文件的名称 属性值任意-->
<!-- 用了Mapper接口代理方法开发-->
<!-- 映射文件中的namespace属性值改为mapper接口UserMapper的完整包名-->
<mapper namespace="com.wmxkb.mapper.AssetMapper">
    <!-- 写入数据-->
    <insert id="write_date" >
        insert into asset(date , value)VALUES(#{param1}, #{param2})
    </insert>
    <!-- 读取所有数据-->
    <select id="read_all"  resultType="com.wmxkb.entity.Asset">
        select * from asset
    </select>
</mapper>
